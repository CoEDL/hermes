'''
Run tests with pytest in ./hermes root directory, or python -m pytest.

pytest --cov src/ for coverage outside PyCharm.

-v for verbose, -s to output print

export PYTHONPATH=".:./src:$PYTHONPATH" if path does not exist.
'''
